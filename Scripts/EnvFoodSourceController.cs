using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnvFoodSourceController : MonoBehaviour
{
    //public Generadores[] fuentes_energia = { Generadores.Sol }; //PP

    public float energia_disponible = 50f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
